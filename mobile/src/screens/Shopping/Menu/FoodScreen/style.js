import { StyleSheet, Dimensions } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  },
  foodList: {
    flex: 1,
    marginTop: 20
  },
  loading: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  }
})
